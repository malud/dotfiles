#!/usr/bin/env bash
{{ if ne .chezmoi.os "darwin" -}}
set -euo pipefail

echo "📦 Installing modern CLI tools for Linux..."

# Detect package manager
if command -v apt-get &>/dev/null; then
  PKG_MGR="apt"
elif command -v dnf &>/dev/null; then
  PKG_MGR="dnf"
elif command -v yum &>/dev/null; then
  PKG_MGR="yum"
elif command -v apk &>/dev/null; then
  PKG_MGR="apk"
else
  echo "⚠️  No supported package manager found"
  PKG_MGR="none"
fi

# Determine if we need sudo
USE_SUDO=false
if [ "$(id -u)" -ne 0 ] && command -v sudo &>/dev/null; then
  USE_SUDO=true
fi


# Install tools based on package manager
case "$PKG_MGR" in
  apt)
    echo "📦 Using apt-get..."
    if [ "$USE_SUDO" = true ]; then
      sudo apt-get update
    else
      apt-get update
    fi

    # bat (often packaged as batcat)
    if ! command -v bat &>/dev/null && ! command -v batcat &>/dev/null; then
      if [ "$USE_SUDO" = true ]; then
        sudo apt-get install -y bat
      else
        apt-get install -y bat
      fi
      # Create symlink if installed as batcat
      if command -v batcat &>/dev/null && ! command -v bat &>/dev/null; then
        mkdir -p "$HOME/.local/bin"
        ln -sf "$(which batcat)" "$HOME/.local/bin/bat"
      fi
    fi

    # fd (often packaged as fd-find)
    if ! command -v fd &>/dev/null && ! command -v fdfind &>/dev/null; then
      if [ "$USE_SUDO" = true ]; then
        sudo apt-get install -y fd-find
      else
        apt-get install -y fd-find
      fi
      # Create symlink if installed as fdfind
      if command -v fdfind &>/dev/null && ! command -v fd &>/dev/null; then
        mkdir -p "$HOME/.local/bin"
        ln -sf "$(which fdfind)" "$HOME/.local/bin/fd"
      fi
    fi

    # ripgrep
    if ! command -v rg &>/dev/null; then
      if [ "$USE_SUDO" = true ]; then
        sudo apt-get install -y ripgrep
      else
        apt-get install -y ripgrep
      fi
    fi

    # fzf
    if ! command -v fzf &>/dev/null; then
      if [ "$USE_SUDO" = true ]; then
        sudo apt-get install -y fzf
      else
        apt-get install -y fzf
      fi
    fi

    # Essential development tools

    # Install Neovim (using AppImage for latest version - required for lazy.nvim which needs >= 0.8.0)
    if ! command -v nvim &>/dev/null; then
      echo "📦 Installing Neovim (latest stable via AppImage)..."
      mkdir -p "$HOME/.local/bin"

      # Download AppImage to temp directory first
      NVIM_TMP=$(mktemp -d)
      if ! curl -sLo "$NVIM_TMP/nvim.appimage" https://github.com/neovim/neovim/releases/latest/download/nvim.appimage; then
        echo "⚠️ Failed to download Neovim AppImage, falling back to apt"
        rm -rf "$NVIM_TMP"

        # Fallback to apt immediately
        if [ "$USE_SUDO" = true ]; then
          sudo apt-get install -y software-properties-common || true
          sudo add-apt-repository -y ppa:neovim-ppa/unstable || true
          sudo apt-get update || true
          sudo apt-get install -y neovim || true
        else
          apt-get install -y neovim || true
        fi
      else
        chmod +x "$NVIM_TMP/nvim.appimage"

        # Check if AppImage works, if not extract it
        if "$NVIM_TMP/nvim.appimage" --version &>/dev/null; then
          echo "📦 AppImage works, installing..."
          mv "$NVIM_TMP/nvim.appimage" "$HOME/.local/bin/nvim"
          rm -rf "$NVIM_TMP"
        else
          echo "📦 AppImage not supported (FUSE unavailable), extracting..."

          # Temporarily disable exit on error for extraction
          set +e

          # Save current directory
          PREV_DIR=$(pwd)
          cd "$NVIM_TMP" || {
            set -e
            rm -rf "$NVIM_TMP"
            echo "⚠️ Failed to change directory"
            if [ "$USE_SUDO" = true ]; then
              sudo apt-get install -y neovim || true
            else
              apt-get install -y neovim || true
            fi
            cd "$PREV_DIR"
            return 0
          }

          # Extract AppImage - this creates squashfs-root directory
          echo "📦 Running extraction (this may take a moment)..."
          ./nvim.appimage --appimage-extract
          EXTRACT_STATUS=$?

          # Re-enable exit on error
          set -e

          if [ $EXTRACT_STATUS -eq 0 ] && [ -d "squashfs-root" ]; then
            echo "✓ Extraction successful"
            mv squashfs-root "$HOME/.local/bin/nvim-squashfs"
            ln -sf "$HOME/.local/bin/nvim-squashfs/usr/bin/nvim" "$HOME/.local/bin/nvim"
            cd "$PREV_DIR"
            rm -rf "$NVIM_TMP"
          else
            echo "⚠️ AppImage extraction failed (exit code: $EXTRACT_STATUS)"
            cd "$PREV_DIR"
            rm -rf "$NVIM_TMP"

            # Fallback: try to install via apt (might be older version)
            echo "📦 Falling back to apt installation..."
            if [ "$USE_SUDO" = true ]; then
              sudo apt-get install -y software-properties-common || true
              sudo add-apt-repository -y ppa:neovim-ppa/unstable || true
              sudo apt-get update || true
              sudo apt-get install -y neovim || true
            else
              apt-get install -y neovim || true
            fi
          fi
        fi
      fi

      # Final check if nvim is now available
      if ! command -v nvim &>/dev/null; then
        echo "⚠️ Neovim installation failed - trying last resort build from source"
        echo "ℹ️  This may take several minutes..."

        # Install build dependencies
        if [ "$USE_SUDO" = true ]; then
          sudo apt-get install -y ninja-build gettext cmake unzip curl build-essential || true
        else
          apt-get install -y ninja-build gettext cmake unzip curl build-essential || true
        fi

        # Try building from source as absolute last resort
        set +e
        git clone https://github.com/neovim/neovim /tmp/neovim || true
        if [ -d "/tmp/neovim" ]; then
          cd /tmp/neovim
          git checkout stable
          make CMAKE_BUILD_TYPE=RelWithDebInfo
          if [ "$USE_SUDO" = true ]; then
            sudo make install || {
              echo "⚠️ All Neovim installation methods failed"
              cd "$PREV_DIR"
              rm -rf /tmp/neovim
              set -e
              return 0
            }
          else
            make install || {
              echo "⚠️ All Neovim installation methods failed"
              cd "$PREV_DIR"
              rm -rf /tmp/neovim
              set -e
              return 0
            }
          fi
          cd "$PREV_DIR"
          rm -rf /tmp/neovim
        fi
        set -e
      fi
    fi

    if ! command -v tmux &>/dev/null; then
      if [ "$USE_SUDO" = true ]; then
        sudo apt-get install -y tmux
      else
        apt-get install -y tmux
      fi
    fi

    if ! command -v git &>/dev/null; then
      if [ "$USE_SUDO" = true ]; then
        sudo apt-get install -y git
      else
        apt-get install -y git
      fi
    fi

    if ! command -v jq &>/dev/null; then
      if [ "$USE_SUDO" = true ]; then
        sudo apt-get install -y jq
      else
        apt-get install -y jq
      fi
    fi

    # yq (Go version - better than Python version)
    if ! command -v yq &>/dev/null; then
      echo "📦 Installing yq..."
      YQ_VERSION="v4.44.1"
      YQ_BINARY="yq_linux_amd64"
      curl -sL "https://github.com/mikefarah/yq/releases/download/${YQ_VERSION}/${YQ_BINARY}" -o "$HOME/.local/bin/yq"
      chmod +x "$HOME/.local/bin/yq"
    fi

    # kubectl (if not already installed)
    if ! command -v kubectl &>/dev/null; then
      echo "📦 Installing kubectl..."
      curl -sLO "https://dl.k8s.io/release/$(curl -sL https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"
      chmod +x kubectl
      mv kubectl "$HOME/.local/bin/"
    fi

    # krew (kubectl plugin manager)
    if ! command -v kubectl-krew &>/dev/null; then
      echo "📦 Installing krew..."
      (
        set -x; cd "$(mktemp -d)" &&
        OS="$(uname | tr '[:upper:]' '[:lower:]')" &&
        ARCH="$(uname -m | sed -e 's/x86_64/amd64/' -e 's/\(arm\)\(64\)\?.*/\1\2/' -e 's/aarch64$/arm64/')" &&
        KREW="krew-${OS}_${ARCH}" &&
        curl -fsSLO "https://github.com/kubernetes-sigs/krew/releases/latest/download/${KREW}.tar.gz" &&
        tar zxvf "${KREW}.tar.gz" &&
        ./"${KREW}" install krew
      )
      export PATH="${KREW_ROOT:-$HOME/.krew}/bin:$PATH"
    fi

    # kustomize with helm plugin
    if ! command -v kustomize &>/dev/null; then
      echo "📦 Installing kustomize..."
      curl -s "https://raw.githubusercontent.com/kubernetes-sigs/kustomize/master/hack/install_kustomize.sh" | bash
      mv kustomize "$HOME/.local/bin/"

      # Install helm plugin for kustomize
      echo "📦 Installing kustomize helm plugin..."
      mkdir -p "$HOME/.config/kustomize/plugin"
      cd "$HOME/.config/kustomize/plugin"
      kustomize plugin install https://github.com/kubernetes-sigs/kustomize/releases/download/kustomize/v5.0.0/kustomize_v5.0.0_linux_amd64.tar.gz || true
    fi

    # k9s
    if ! command -v k9s &>/dev/null; then
      echo "📦 Installing k9s..."
      K9S_VERSION="v0.32.5"
      curl -sL "https://github.com/derailed/k9s/releases/download/${K9S_VERSION}/k9s_Linux_amd64.tar.gz" -o /tmp/k9s.tar.gz
      tar -xzf /tmp/k9s.tar.gz -C /tmp
      mv /tmp/k9s "$HOME/.local/bin/"
      rm /tmp/k9s.tar.gz
    fi

    # eza (modern ls replacement)
    if ! command -v eza &>/dev/null; then
      echo "📦 Installing eza..."
      EZA_VERSION="v0.20.11"
      ARCH="$(uname -m)"
      if [ "$ARCH" = "x86_64" ]; then
        EZA_ARCH="x86_64"
      elif [ "$ARCH" = "aarch64" ]; then
        EZA_ARCH="aarch64"
      else
        EZA_ARCH="x86_64"
      fi
      curl -sL "https://github.com/eza-community/eza/releases/download/${EZA_VERSION}/eza_${EZA_ARCH}-unknown-linux-gnu.tar.gz" -o /tmp/eza.tar.gz
      tar -xzf /tmp/eza.tar.gz -C /tmp
      mv /tmp/eza "$HOME/.local/bin/"
      rm /tmp/eza.tar.gz
    fi

    # zoxide (smarter cd)
    if ! command -v zoxide &>/dev/null; then
      echo "📦 Installing zoxide..."
      ZOXIDE_VERSION="v0.9.6"
      ARCH="$(uname -m)"
      if [ "$ARCH" = "x86_64" ]; then
        ZOXIDE_ARCH="x86_64"
      elif [ "$ARCH" = "aarch64" ]; then
        ZOXIDE_ARCH="aarch64"
      else
        ZOXIDE_ARCH="x86_64"
      fi
      curl -sL "https://github.com/ajeetdsouza/zoxide/releases/download/${ZOXIDE_VERSION}/zoxide-${ZOXIDE_VERSION}-${ZOXIDE_ARCH}-unknown-linux-musl.tar.gz" -o /tmp/zoxide.tar.gz
      tar -xzf /tmp/zoxide.tar.gz -C /tmp
      mv /tmp/zoxide "$HOME/.local/bin/"
      rm /tmp/zoxide.tar.gz
    fi
    ;;

  dnf|yum)
    echo "📦 Using $PKG_MGR..."

    if [ "$USE_SUDO" = true ]; then
      command -v bat &>/dev/null || sudo "$PKG_MGR" install -y bat
      command -v fd &>/dev/null || sudo "$PKG_MGR" install -y fd-find
      command -v rg &>/dev/null || sudo "$PKG_MGR" install -y ripgrep
      command -v fzf &>/dev/null || sudo "$PKG_MGR" install -y fzf
    else
      command -v bat &>/dev/null || "$PKG_MGR" install -y bat
      command -v fd &>/dev/null || "$PKG_MGR" install -y fd-find
      command -v rg &>/dev/null || "$PKG_MGR" install -y ripgrep
      command -v fzf &>/dev/null || "$PKG_MGR" install -y fzf
    fi

    # eza (modern ls replacement)
    if ! command -v eza &>/dev/null; then
      echo "📦 Installing eza..."
      EZA_VERSION="v0.20.11"
      ARCH="$(uname -m)"
      if [ "$ARCH" = "x86_64" ]; then
        EZA_ARCH="x86_64"
      elif [ "$ARCH" = "aarch64" ]; then
        EZA_ARCH="aarch64"
      else
        EZA_ARCH="x86_64"
      fi
      mkdir -p "$HOME/.local/bin"
      curl -sL "https://github.com/eza-community/eza/releases/download/${EZA_VERSION}/eza_${EZA_ARCH}-unknown-linux-gnu.tar.gz" -o /tmp/eza.tar.gz
      tar -xzf /tmp/eza.tar.gz -C /tmp
      mv /tmp/eza "$HOME/.local/bin/"
      rm /tmp/eza.tar.gz
    fi

    # zoxide (smarter cd)
    if ! command -v zoxide &>/dev/null; then
      echo "📦 Installing zoxide..."
      ZOXIDE_VERSION="v0.9.6"
      ARCH="$(uname -m)"
      if [ "$ARCH" = "x86_64" ]; then
        ZOXIDE_ARCH="x86_64"
      elif [ "$ARCH" = "aarch64" ]; then
        ZOXIDE_ARCH="aarch64"
      else
        ZOXIDE_ARCH="x86_64"
      fi
      mkdir -p "$HOME/.local/bin"
      curl -sL "https://github.com/ajeetdsouza/zoxide/releases/download/${ZOXIDE_VERSION}/zoxide-${ZOXIDE_VERSION}-${ZOXIDE_ARCH}-unknown-linux-musl.tar.gz" -o /tmp/zoxide.tar.gz
      tar -xzf /tmp/zoxide.tar.gz -C /tmp
      mv /tmp/zoxide "$HOME/.local/bin/"
      rm /tmp/zoxide.tar.gz
    fi
    ;;

  apk)
    echo "📦 Using apk (Alpine)..."

    if [ "$USE_SUDO" = true ]; then
      command -v bat &>/dev/null || sudo apk add bat
      command -v fd &>/dev/null || sudo apk add fd
      command -v rg &>/dev/null || sudo apk add ripgrep
      command -v fzf &>/dev/null || sudo apk add fzf
    else
      command -v bat &>/dev/null || apk add bat
      command -v fd &>/dev/null || apk add fd
      command -v rg &>/dev/null || apk add ripgrep
      command -v fzf &>/dev/null || apk add fzf
    fi

    # eza (modern ls replacement)
    if ! command -v eza &>/dev/null; then
      echo "📦 Installing eza..."
      EZA_VERSION="v0.20.11"
      ARCH="$(uname -m)"
      if [ "$ARCH" = "x86_64" ]; then
        EZA_ARCH="x86_64"
      elif [ "$ARCH" = "aarch64" ]; then
        EZA_ARCH="aarch64"
      else
        EZA_ARCH="x86_64"
      fi
      mkdir -p "$HOME/.local/bin"
      curl -sL "https://github.com/eza-community/eza/releases/download/${EZA_VERSION}/eza_${EZA_ARCH}-unknown-linux-musl.tar.gz" -o /tmp/eza.tar.gz
      tar -xzf /tmp/eza.tar.gz -C /tmp
      mv /tmp/eza "$HOME/.local/bin/"
      rm /tmp/eza.tar.gz
    fi

    # zoxide (smarter cd)
    if ! command -v zoxide &>/dev/null; then
      echo "📦 Installing zoxide..."
      ZOXIDE_VERSION="v0.9.6"
      ARCH="$(uname -m)"
      if [ "$ARCH" = "x86_64" ]; then
        ZOXIDE_ARCH="x86_64"
      elif [ "$ARCH" = "aarch64" ]; then
        ZOXIDE_ARCH="aarch64"
      else
        ZOXIDE_ARCH="x86_64"
      fi
      mkdir -p "$HOME/.local/bin"
      curl -sL "https://github.com/ajeetdsouza/zoxide/releases/download/${ZOXIDE_VERSION}/zoxide-${ZOXIDE_VERSION}-${ZOXIDE_ARCH}-unknown-linux-musl.tar.gz" -o /tmp/zoxide.tar.gz
      tar -xzf /tmp/zoxide.tar.gz -C /tmp
      mv /tmp/zoxide "$HOME/.local/bin/"
      rm /tmp/zoxide.tar.gz
    fi
    ;;

  none)
    echo "📦 Installing all tools via pre-built binaries..."

    mkdir -p "$HOME/.local/bin"

    # bat
    if ! command -v bat &>/dev/null; then
      echo "📦 Installing bat..."
      BAT_VERSION="v0.24.0"
      ARCH="$(uname -m)"
      if [ "$ARCH" = "x86_64" ]; then
        BAT_ARCH="x86_64"
      elif [ "$ARCH" = "aarch64" ]; then
        BAT_ARCH="aarch64"
      else
        BAT_ARCH="x86_64"
      fi
      curl -sL "https://github.com/sharkdp/bat/releases/download/${BAT_VERSION}/bat-${BAT_VERSION}-${BAT_ARCH}-unknown-linux-musl.tar.gz" -o /tmp/bat.tar.gz
      tar -xzf /tmp/bat.tar.gz -C /tmp
      mv /tmp/bat-${BAT_VERSION}-${BAT_ARCH}-unknown-linux-musl/bat "$HOME/.local/bin/"
      rm -rf /tmp/bat.tar.gz /tmp/bat-${BAT_VERSION}-${BAT_ARCH}-unknown-linux-musl
    fi

    # fd
    if ! command -v fd &>/dev/null; then
      echo "📦 Installing fd..."
      FD_VERSION="v10.2.0"
      ARCH="$(uname -m)"
      if [ "$ARCH" = "x86_64" ]; then
        FD_ARCH="x86_64"
      elif [ "$ARCH" = "aarch64" ]; then
        FD_ARCH="aarch64"
      else
        FD_ARCH="x86_64"
      fi
      curl -sL "https://github.com/sharkdp/fd/releases/download/${FD_VERSION}/fd-${FD_VERSION}-${FD_ARCH}-unknown-linux-musl.tar.gz" -o /tmp/fd.tar.gz
      tar -xzf /tmp/fd.tar.gz -C /tmp
      mv /tmp/fd-${FD_VERSION}-${FD_ARCH}-unknown-linux-musl/fd "$HOME/.local/bin/"
      rm -rf /tmp/fd.tar.gz /tmp/fd-${FD_VERSION}-${FD_ARCH}-unknown-linux-musl
    fi

    # ripgrep
    if ! command -v rg &>/dev/null; then
      echo "📦 Installing ripgrep..."
      RG_VERSION="14.1.1"
      ARCH="$(uname -m)"
      if [ "$ARCH" = "x86_64" ]; then
        RG_ARCH="x86_64"
      elif [ "$ARCH" = "aarch64" ]; then
        RG_ARCH="aarch64"
      else
        RG_ARCH="x86_64"
      fi
      curl -sL "https://github.com/BurntSushi/ripgrep/releases/download/${RG_VERSION}/ripgrep-${RG_VERSION}-${RG_ARCH}-unknown-linux-musl.tar.gz" -o /tmp/rg.tar.gz
      tar -xzf /tmp/rg.tar.gz -C /tmp
      mv /tmp/ripgrep-${RG_VERSION}-${RG_ARCH}-unknown-linux-musl/rg "$HOME/.local/bin/"
      rm -rf /tmp/rg.tar.gz /tmp/ripgrep-${RG_VERSION}-${RG_ARCH}-unknown-linux-musl
    fi

    # eza
    if ! command -v eza &>/dev/null; then
      echo "📦 Installing eza..."
      EZA_VERSION="v0.20.11"
      ARCH="$(uname -m)"
      if [ "$ARCH" = "x86_64" ]; then
        EZA_ARCH="x86_64"
      elif [ "$ARCH" = "aarch64" ]; then
        EZA_ARCH="aarch64"
      else
        EZA_ARCH="x86_64"
      fi
      curl -sL "https://github.com/eza-community/eza/releases/download/${EZA_VERSION}/eza_${EZA_ARCH}-unknown-linux-musl.tar.gz" -o /tmp/eza.tar.gz
      tar -xzf /tmp/eza.tar.gz -C /tmp
      mv /tmp/eza "$HOME/.local/bin/"
      rm /tmp/eza.tar.gz
    fi

    # zoxide
    if ! command -v zoxide &>/dev/null; then
      echo "📦 Installing zoxide..."
      ZOXIDE_VERSION="v0.9.6"
      ARCH="$(uname -m)"
      if [ "$ARCH" = "x86_64" ]; then
        ZOXIDE_ARCH="x86_64"
      elif [ "$ARCH" = "aarch64" ]; then
        ZOXIDE_ARCH="aarch64"
      else
        ZOXIDE_ARCH="x86_64"
      fi
      curl -sL "https://github.com/ajeetdsouza/zoxide/releases/download/${ZOXIDE_VERSION}/zoxide-${ZOXIDE_VERSION}-${ZOXIDE_ARCH}-unknown-linux-musl.tar.gz" -o /tmp/zoxide.tar.gz
      tar -xzf /tmp/zoxide.tar.gz -C /tmp
      mv /tmp/zoxide "$HOME/.local/bin/"
      rm /tmp/zoxide.tar.gz
    fi

    # fzf via git
    if ! command -v fzf &>/dev/null; then
      git clone --depth 1 https://github.com/junegunn/fzf.git ~/.fzf
      ~/.fzf/install --bin
    fi
    ;;
esac

# Install starship (common to all platforms)
if ! command -v starship &>/dev/null; then
  echo "📦 Installing starship..."
  curl -sS https://starship.rs/install.sh | sh -s -- -y
fi

echo ""
echo "✅ Modern CLI tools installed!"
echo ""
echo "Tools available:"
command -v eza &>/dev/null && echo "  ✓ eza (better ls)"
command -v bat &>/dev/null && echo "  ✓ bat (better cat)"
command -v fd &>/dev/null && echo "  ✓ fd (better find)"
command -v rg &>/dev/null && echo "  ✓ rg (better grep)"
command -v fzf &>/dev/null && echo "  ✓ fzf (fuzzy finder)"
command -v zoxide &>/dev/null && echo "  ✓ zoxide (smarter cd)"
command -v starship &>/dev/null && echo "  ✓ starship (prompt)"
command -v nvim &>/dev/null && echo "  ✓ neovim (editor)"
command -v tmux &>/dev/null && echo "  ✓ tmux (terminal multiplexer)"
command -v git &>/dev/null && echo "  ✓ git (version control)"
command -v jq &>/dev/null && echo "  ✓ jq (JSON processor)"
command -v yq &>/dev/null && echo "  ✓ yq (YAML processor)"
command -v kubectl &>/dev/null && echo "  ✓ kubectl (kubernetes CLI)"
command -v kubectl-krew &>/dev/null && echo "  ✓ krew (kubectl plugin manager)"
command -v kustomize &>/dev/null && echo "  ✓ kustomize (kubernetes config management with helm plugin)"
command -v k9s &>/dev/null && echo "  ✓ k9s (kubernetes TUI)"
echo ""
echo "ℹ️  Note: Nerd Font icons are rendered by your local terminal/VSCode"
echo "   Ensure 'SauceCodePro Nerd Font' is installed on your host system (macOS)"

{{ else -}}
echo "⚠️  This script is for non-macOS systems only"
echo "ℹ️  On macOS, use Homebrew: brew bundle"
exit 0
{{ end -}}
